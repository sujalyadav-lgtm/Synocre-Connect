// Firebase config stub — Firebase is no longer used.
// Auth and Firestore are handled by the local-first adapter (src/lib/local-db.ts).
export const app = null;
export const db = null;
export const auth = null;
